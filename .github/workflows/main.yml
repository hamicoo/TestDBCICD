name: Hello HAMICOO

on:
  workflow_dispatch:   # run manually

jobs:
  hello:
    runs-on: [self-hosted, HAMICOO]
    steps:
      - name: Say Hello
        run: echo "Hello from runner HAMICOO 🎉"
